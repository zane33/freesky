reflex==0.9.7
curl-cffi==0.15.0
# fastapi/uvicorn/httpx are reflex dependencies too — left unpinned so reflex's own
# constraints resolve. Pinning them to 0.8.0-era versions made the resolve impossible.
httpx[http2]
python-dateutil==2.9.0.post0
uvicorn[standard]
aiohttp==3.14.1
fastapi
playwright==1.61.0
